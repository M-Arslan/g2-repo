/**
 * @typedef {object} AdjusterLicenseStates
 * @property {string} adjusterLicenseStateId
 * @property {number} state
 * @property {boolean} active
 */

/**
 * Actions for the AdjusterLicenseStates slice
 *
 * @typedef {object} AdjusterLicenseStates
 * @property {Function} get
 */

/** 
 * AdjusterLicenseStates selectors
 * 
 * @typedef {import('../../types.d').BaseListSelectors<AdjusterLicenseStates>} AdjusterLicenseStatesSelectors
 */

/** @typedef {import('../../types.d').StateSlice<AdjusterLicenseStatesActions, AdjusterLicenseStatesSelectors>} AdjusterLicenseStatesSlice */

