/**
 * @typedef {object} CheckExaminerForAdjusterLicenseState
 * @property {boolean} Result
 */

/**
 * Actions for the CheckExaminerForAdjusterLicenseState slice
 *
 * @typedef {object} CheckExaminerForAdjusterLicenseStateActions
 * @property {Function} get
 */

/** 
 * Risk State selectors
 * 
 * @typedef {import('../../types.d').BaseListSelectors<CheckExaminerForAdjusterLicenseState>} CheckExaminerForAdjusterLicenseStateSelectors
 */

/** @typedef {import('../../types.d').StateSlice<CheckExaminerForAdjusterLicenseStateActions, CheckExaminerForAdjusterLicenseStateSelectors>} CheckExaminerForAdjusterLicenseStateSlice */

export const KEY = 'CheckExaminerForAdjusterLicenseState';