import styled from 'styled-components';
import {
    Divider,
} from '@mui/material';

export const FormDivider = styled(Divider)` margin-bottom: 1em !important; `;

export const FieldContainer = styled.div`
    width: 100%;
    min-height: 50px;
    height: auto;
    padding: .25em 1em;
    margin-bottom: 1em;
`;
